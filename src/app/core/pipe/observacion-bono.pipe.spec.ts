import { ObservacionBonoPipe } from './observacion-bono.pipe';

describe('ObservacionBonoPipe', () => {
  it('create an instance', () => {
    const pipe = new ObservacionBonoPipe();
    expect(pipe).toBeTruthy();
  });
});
